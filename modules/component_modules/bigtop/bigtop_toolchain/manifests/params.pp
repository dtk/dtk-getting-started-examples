class bigtop_toolchain::params()
{
  # TODO: first step in fixing parameterization
  $ant_version = '1.9.6'
  $ant_package_name = "apache-ant-${ant_version}"
  $ant_bin_tar = "${ant_package_name}-bin.tar.gz" 
}